# Example environment variables for this project
# Set these in Vercel (Project > Settings > Environment Variables)

# MongoDB connection string for production
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.example.mongodb.net/style?retryWrites=true&w=majority

# Node environment
NODE_ENV=production

# Optional local port override
PORT=5000

# Any app secrets used by your app (e.g. JWT secret)
#JWT_SECRET=your_jwt_secret_here

# Add other provider keys or secrets below
#API_KEY=your_api_key_here
